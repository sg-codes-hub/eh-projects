(()=>{
'use strict';
const KEY='eh15e-student-progress-v1';
const load=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'{}')}catch{return {}}};
const save=p=>{try{localStorage.setItem(KEY,JSON.stringify(p))}catch{}};
const update=patch=>{const p={...load(),...patch,lastUpdated:Date.now()};save(p);return p};
const esc=s=>String(s??'').replace(/[<>&\"']/g,c=>({'<':'&lt;','>':'&gt;','&':'&amp;','\"':'&quot;',"'":'&#39;'}[c]));
window.EHProgress={get:load,clear:()=>localStorage.removeItem(KEY),saveSession:r=>update({resume:{...r,savedAt:Date.now()}}),clearSession:()=>{const p=load();delete p.resume;save(p)},getStats:()=>{const p=load(),a=Object.values(p.attempted||{});return{attempted:a.length,correct:a.filter(x=>x.correct).length,wrong:a.filter(x=>x.correct===false).length}}};
function currentSession(){const practice=document.getElementById('practice'),title=document.getElementById('practiceTitle'),progress=document.getElementById('progressText');if(!practice||!practice.classList.contains('active'))return null;const raw=(progress?.textContent||'').trim();const m=raw.match(/Question\s+(\d+)\s+of\s+(\d+)/i);if(!m)return null;const pills=[...document.querySelectorAll('#quizArea .question-meta .pill')].map(x=>x.textContent.trim()).filter(Boolean);const eyebrow=(document.getElementById('practiceEyebrow')?.textContent||'').toUpperCase();let mode='chapter';let marks=null;if(eyebrow.includes('MOCK'))mode='mock';else if(/^[1-5]-MARK\s+PRACTICE$/.test(eyebrow)){mode='marks';marks=Number((eyebrow.match(/^([1-5])-MARK/)||[])[1]);}return{mode,title:title?.textContent||'',i:Math.max(0,+m[1]-1),total:+m[2],marks,module:pills[0]||'',chapter:pills[1]||''}}
function saveCurrent(){const r=currentSession();if(r)window.EHProgress.saveSession(r)}
function findButton(selector,text){return [...document.querySelectorAll(selector)].find(b=>(b.textContent||'').trim().toLowerCase().includes(String(text||'').trim().toLowerCase()))}
function advanceTo(target){let n=Number(target||0),tries=0;const advance=()=>{if(n<=0)return;const next=document.getElementById('nextBtn');if(!next||next.disabled){if(++tries<100)setTimeout(advance,100);return}n--;next.click();setTimeout(advance,120)};setTimeout(advance,500)}
function resumeStudentProgress(r){if(!r)return;const target=Number(r.i||0);
if(r.mode==='mock'){const b=findButton('button','mock');if(!b)return;b.click();let tries=0;const jump=()=>{const btn=document.querySelector(`#questionPalette [data-p="${target}"]`);if(btn){btn.click();return}if(++tries<60)setTimeout(jump,100)};setTimeout(jump,600);return}
if(r.mode==='marks'){const b=[...document.querySelectorAll('.quick-card[data-mode="marks"]')].find(x=>Number(x.dataset.marks)===Number(r.marks));if(!b)return;b.click();advanceTo(target);return}
const moduleName=String(r.module||'').toLowerCase();
let section=[...document.querySelectorAll('[data-sec]')].find(b=>{const t=(b.textContent||'').toLowerCase();return moduleName&& (t.includes(moduleName)||moduleName.includes(t))});
if(!section)section=[...document.querySelectorAll('[data-sec]')].find(b=>(b.dataset.sec||'').toLowerCase()===moduleName);
if(!section)return;section.click();let tries=0;const openChapter=()=>{const chapter=String(r.chapter||'').toLowerCase();const b=[...document.querySelectorAll('.chapter-btn:not([disabled])')].find(x=>{const t=(x.dataset.c||x.textContent||'').toLowerCase();return chapter&&(t===chapter||t.includes(chapter)||chapter.includes(t))});if(!b){if(++tries<60)setTimeout(openChapter,100);return}b.click();advanceTo(target)};setTimeout(openChapter,250)}
window.resumeStudentProgress=resumeStudentProgress;
function resumeCard(){const r=load().resume;if(!r||Date.now()-Number(r.savedAt||0)>30*24*60*60*1000)return null;const el=document.createElement('section');el.className='resume-progress-card';el.innerHTML=`<div class="rp-icon">🎯</div><div class="rp-main"><span class="eyebrow">CONTINUE PRACTISING</span><h2>${esc(r.title||r.chapter||`${r.marks||''}-Mark Practice`)}</h2><p>You stopped at question ${Number(r.i||0)+1}${r.total?' of '+r.total:''}. Continue where you left off.</p></div><button class="primary-btn" type="button" id="resumeProgressBtn">Continue →</button>`;return{el,r}}
function inject(){const dashboard=document.getElementById('dashboard'),hero=dashboard?.querySelector('.hero');if(!dashboard||!hero||document.getElementById('resumeProgressCard'))return;const x=resumeCard();if(!x)return;x.el.id='resumeProgressCard';hero.insertAdjacentElement('afterend',x.el);x.el.querySelector('#resumeProgressBtn').onclick=()=>resumeStudentProgress(x.r)}
function observe(){document.addEventListener('click',e=>{if(e.target.closest('#nextBtn,#prevBtn,[data-p]'))setTimeout(saveCurrent,180);if(e.target.closest('#quitBtn'))setTimeout(()=>{saveCurrent();inject()},150);if(e.target.closest('#resultHomeBtn,#homeBtn,#backBtn,#syllabusBackBtn'))setTimeout(()=>window.EHProgress.clearSession(),100)});setInterval(()=>{saveCurrent();inject()},500);window.addEventListener('pagehide',saveCurrent);window.addEventListener('beforeunload',saveCurrent)}
function boot(){observe();inject();setTimeout(inject,1000);setTimeout(inject,2500)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();