/* Bank fetches are now handled directly by question-bank-loader.js.
   Keeping this shim as a no-op preserves the existing script contract without
   cloning/parsing every bank response a second time. */
(()=>{})();
