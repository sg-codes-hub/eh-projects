const list=document.getElementById('list'),count=document.getElementById('count');let items=[];try{items=JSON.parse(localStorage.getItem('eh15e.mistakes')||'[]')}catch{};count.textContent=items.length?`${items.length} question${items.length===1?'':'s'} to revisit`:'No mistakes yet — excellent!';if(!items.length){list.innerHTML='<div class="empty-state"><div class="empty-icon">🎉</div><h2>Your review list is clear</h2><p>When you miss an objective question during practice, it will appear here.</p></div>'}else{list.innerHTML=items.slice().reverse().map((x,i)=>`<article class="mistake-card"><div class="mistake-head"><span class="card-label">${x.title||x.mode||'Practice'}</span><span class="mistake-tag">Review</span></div><h2>${i+1}. ${esc(x.prompt)}</h2><div class="review-answer"><strong>Correct answer:</strong> ${esc(Array.isArray(x.options)?x.options[Number(x.answer)]:(x.answer||''))}</div>${x.explanation?`<p><strong>Why:</strong> ${esc(x.explanation)}</p>`:''}</article>`).join('');const clear=document.createElement('button');clear.className='clear-button';clear.textContent='Clear Review List';clear.onclick=()=>{if(confirm('Clear all saved mistakes?')){localStorage.removeItem('eh15e.mistakes');location.reload()}};list.appendChild(clear)}function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}